import 'package:flutter/material.dart';

class HourlyWeatherTitle extends StatelessWidget {
  const HourlyWeatherTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}